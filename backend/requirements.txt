Werkzeug==2.2.2
Flask==2.2.2
Flask_Cors==3.0.10
Werkzeug==2.3.7 # TODO update Flask and unpin this
# if bcrypt gives TypeError: uninstall python_bcrypt==0.3.2 and pip install bcrypt (and vice versa)
bcrypt